
export const BUTTON = {
    COLOR  : "primary",     // primary || secondary || tertiary || info || success || warning || danger 
    SIZE   : "",            // btn-sm || '' || btn-lg
    BORDER: "",            // '' || border-dark || border-dark-lg
}
